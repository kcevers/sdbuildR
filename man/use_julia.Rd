% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_julia.R
\name{use_julia}
\alias{use_julia}
\title{Start Julia and activate environment}
\usage{
use_julia(stop = FALSE, force = FALSE, nthreads = NULL)
}
\arguments{
\item{stop}{If \code{TRUE}, stop active Julia session. Defaults to \code{FALSE}.}

\item{force}{If \code{TRUE}, force Julia setup to execute again.}

\item{nthreads}{If not \code{NULL}, set the number of threads for Julia to use. This will set the environment variable \code{JULIA_NUM_THREADS} and restart Julia if it is already running to apply the new thread setting. See \href{https://docs.julialang.org/en/v1/manual/parallel-computing/#man-parallel-computing}{this page} for more details on threading in Julia.}
}
\value{
Returns \code{NULL} invisibly, used for side effects
}
\description{
Start Julia session and activate Julia environment to simulate stock-and-flow models. To do so, Julia needs to be installed (see \url{https://julialang.org/install/}) and findable from within R. See \href{https://kcevers.github.io/sdbuildR/articles/julia-setup.html}{this vignette} for guidance. In addition, the Julia environment specifically for sdbuildR needs to have been instantiated. This can be set up with \code{\link[=install_julia_env]{install_julia_env()}}.
}
\details{
Julia supports running stock-and-flow models with units as well as \link[=ensemble]{ensemble simulations}.

In every R session, \code{\link[=use_julia]{use_julia()}} needs to be run once (which is done automatically in \code{\link[=simulate]{simulate()}}), which can take around 30-60 seconds.
}
\examples{
\dontshow{if (is_julia_ready()) withAutoprint(\{ # examplesIf}
# Start a Julia session and activate the Julia environment for sdbuildR
use_julia()

# Start Julia with 4 threads (if your Julia installation supports threading)
use_julia(nthreads = 4)

# Stop Julia session
use_julia(stop = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=install_julia_env]{install_julia_env()}}
}
\concept{julia}
