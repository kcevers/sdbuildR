% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{as.data.frame.ensemble_stockflow}
\alias{as.data.frame.ensemble_stockflow}
\title{Create data frame of simulation results}
\usage{
\method{as.data.frame}{ensemble_stockflow}(
  x,
  row.names = NULL,
  optional = FALSE,
  which = c("summary", "sims")[1],
  direction = "long",
  sim = NULL,
  condition = NULL,
  vars = NULL,
  type = NULL,
  ...
)
}
\arguments{
\item{x}{Output of \code{\link[=simulate.stockflow]{simulate()}}.}

\item{row.names}{NULL or a character vector giving the row names for the data frame. Missing values are not allowed.}

\item{optional}{Ignored parameter.}

\item{which}{Type of data to return. Either \code{"summary"} for a summary statistics, or \code{"sims"} for individual simulation trajectories. Defaults to \code{"summary"}.}

\item{direction}{Format of data frame, either "long" (default) or "wide".}

\item{sim}{Indices of the individual trajectories to include if which = \code{"sims"}. Defaults to \code{NULL}, which includes all trajectories. Including a high number of trajectories will create a large object.}

\item{condition}{Indices of the conditions to include. Defaults to \code{NULL}, which includes all conditions.}

\item{vars}{Variables to plot. Defaults to \code{NULL} to plot all variables.}

\item{type}{Variable types to retain in the data frame. Must be one or more of 'stock', 'flow', 'constant', 'aux', 'gf', or 'func'. Defaults to \code{NULL} to include all types.}

\item{...}{Optional parameters}
}
\value{
A data.frame with simulation results. For \code{direction = "long"} (default),
the data frame has three columns: \code{time}, \code{variable}, and \code{value}.
For \code{direction = "wide"}, the data frame has columns \code{time} followed by
one column per variable.
}
\description{
Convert simulation results to a data.frame.
}
\examples{
sfm <- stockflow("sir")
sims <- ensemble(sfm, n = 10)
df <- as.data.frame(sims)
head(df)

# Get results in wide format
df_wide <- as.data.frame(sims, direction = "wide")
head(df_wide)

}
\seealso{
\code{\link[=ensemble]{ensemble()}}, \code{\link[=stockflow]{stockflow()}}
}
\concept{ensemble}
