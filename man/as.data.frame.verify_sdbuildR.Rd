% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{as.data.frame.verify_sdbuildR}
\alias{as.data.frame.verify_sdbuildR}
\title{Convert verify results to a data frame}
\usage{
\method{as.data.frame}{verify_sdbuildR}(
  x,
  row.names = NULL,
  optional = FALSE,
  nr = NULL,
  label = NULL,
  ignore_case = TRUE,
  status = c("pass", "fail", "error", "skip"),
  ...
)
}
\arguments{
\item{x}{A \code{verify_sdbuildR} object (output of \code{\link[=verify]{verify()}}).}

\item{row.names}{\code{NULL} or a character vector giving row names (optional).}

\item{optional}{Ignored; present for compatibility.}

\item{nr}{Integer vector of test number(s) to display (1-based). Defaults to
\code{NULL} (show all tests). Can be combined with \code{label} (intersection).}

\item{label}{Character vector of regex patterns for partial, case-insensitive
label matching. A test is included if its label matches \emph{any} pattern.
E.g., \code{c("non-neg", "beta")} returns tests matching either fragment.
Can be combined with \code{nr} (intersection).}

\item{ignore_case}{Logical; whether \code{label} matching is case-insensitive.
Default \code{TRUE}.}

\item{status}{Optional character vector of test statuses to include (e.g., \code{c("fail", "error")}). Defaults to \code{c("pass", "fail", "error", "skip")} (include all).}

\item{...}{Additional arguments (unused).}
}
\value{
A \code{data.frame}.
}
\description{
Returns one row per unit test with columns \code{nr}, \code{label}, \code{status}, \code{outcome},
\code{pass_rate}, \code{n_pass}, \code{n_fail}, \code{n_error}, \code{expr_str}, \code{conditions}, and
\code{message}. Use the \code{nr} argument to filter to specific tests.
}
\examples{
sfm <- sdbuildR("SIR") |>
  unit_test(expr = all(susceptible >= 0))
res <- verify(sfm)
as.data.frame(res)
}
\concept{unitTest}
